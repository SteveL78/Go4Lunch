<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- SplashScreen theme. -->
    <style name="AppThemeSplashScreen" parent="Theme.SplashScreen">
        <!-- Set the splash screen background, animated icon, and animation duration. -->
        <item name="windowSplashScreenBackground">@color/orange</item>
        <!-- Use windowSplashScreenAnimatedIcon to add either a drawable or an
             animated drawable. One of these is required. -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_screen</item>
        <!-- Required for animated icons -->
        <item name="windowSplashScreenAnimationDuration">800</item>
        <!-- Set the theme of the Activity that directly follows your splash screen. -->
        <!-- Required -->
        <item name="postSplashScreenTheme">@style/AppTheme</item>
    </style>


    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowFullscreen">true</item>

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/orange</item>
        <item name="colorPrimaryVariant">@color/darkOrange</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color.-->
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color.-->
        <item name="android:statusBarColor" tools:ignore="ObsoleteSdkInt" tools:targetApi="l">
            ?attr/colorPrimaryVariant
        </item>
        <!-- Customize your theme here. -->
    </style>

    <!-- STYLE SIGN-IN ACTIVITY -->
    <style name="LoginTheme" parent="AppTheme" >
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:textColorSecondary">@android:color/darker_gray</item>
        <item name="android:textColorHint">@android:color/darker_gray</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>


    <!-- AUTHENTICATION : Material Button themes -->
    <style name="AuthTheme" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="MaterialButtonThemeFacebook" parent="AuthTheme">
        <item name="android:backgroundTint">@color/color_fb_btn</item>
    </style>

    <style name="MaterialButtonThemeGoogle" parent="AuthTheme">
        <item name="colorPrimary">@color/color_google_btn</item>
    </style>

    <style name="MaterialButtonThemeTwitter" parent="AuthTheme">
        <item name="colorPrimary">@color/color_twitter_btn</item>
    </style>

    <style name="MaterialButtonThemeEmail" parent="AuthTheme">
    </style>


</resources>